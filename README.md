# Aston Project

Учебный проект по реализации универсальной сортировки и поиска для классов `Person`, `Student`, `Teacher`.

## 🧾 Цель проекта  
Разработать Java-приложение, которое:  
- работает с пользовательскими классами (`Student`, `Teacher`) через интерфейс `Person`  
- реализует различные алгоритмы сортировки (например, пузырьком, многопоточно)  
- реализует бинарный поиск и дополнительные функциональные задания (сортировка чёт/нечёт полей, запись в файл)  
- использует паттерн “Стратегия” для сортировок, дженерики, Builder-паттерн для классов  
- предоставляет консольный интерфейс с меню: выбор источника данных, генерация/ввод/файл, установка длины массива, сортировка, поиск, сохранение.

## 📂 Структура проекта  
src/
├─ app/ # Консольное приложение (меню, выбор, запуск)
├─ model/ # Модели: Person, Student, Teacher, SubjectsEnum
├─ util/ # Утилиты: генератор случайных объектов, ввод с консоли и др.
├─ sorting/ # Алгоритмы сортировки + стратегия
├─ search/ # Алгоритм бинарного поиска
├─ io/ # Ввод/вывод: чтение из файла, запись в файл
└─ tests/ # Простые ручные тесты (без JUnit)

## ✅ Основной функционал  
- Генерация коллекции объектов: ручной ввод, случайная генерация, чтение из файла.  
- Сортировка коллекции по одному из алгоритмов: например, пузырьком (`BubbleSort`), многопоточно (`BubbleSortThreadPool`), либо с учётом чёт/нечёт полей.  
- Бинарный поиск по `id`.  
- Запись результатов (отсортированной коллекции) в файл с режимом добавления.  
- Валидация данных: имя/фамилия не содержат цифр, первая буква — заглавная, уникальные id и др.

## 🧮 Дополнительные задания  
1. Сортировка по числовому полю, где **объекты с чётными значениями** участвуют в сортировке и размещаются в порядке, а **с нечётными** остаются на своих местах.  
2. Запись коллекций/результатов в файл.  
3. Заполнение коллекций посредством стримов.  
4. Многопоточный метод подсчёта количества вхождений элемента в коллекцию.

## 🧰 Как запустить  
1. Клонируйте репозиторий:  
   ```bash
   git clone https://github.com/Troloroma/aston_project.git
